[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "volsurface"
version = "0.0.0"
dependencies = [
    "openpyxl>=3.1,<4",
]

[tool.setuptools.packages.find]
where = ["src"]
